# Optional: canonical origin for absolute metadata URLs when deployed
# NEXT_PUBLIC_SITE_URL=https://your-host.example.com

# Emit Strict-Transport-Security only if this app terminates TLS (not required
# when a front proxy already sets HSTS).
# ENABLE_HSTS=true
